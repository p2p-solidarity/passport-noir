[package]
name = "prepare_link"
type = "bin"
authors = ["kidneyweakx"]
description = "OpenAC prepare-phase linking commitment circuit for OpenPassport"

[dependencies]
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
