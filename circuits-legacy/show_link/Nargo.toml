[package]
name = "show_link"
type = "bin"
authors = ["kidneyweakx"]
description = "OpenAC show-phase linking and challenge binding circuit for OpenPassport"

[dependencies]
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
