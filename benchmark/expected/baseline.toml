# Benchmark Baseline
# Generated: 2026-04-01; refreshed 2026-04-26 after openac_core security fixes.
# 2026-04-28: refreshed after closing P0-5 (sdjwt b64 inclusion) and P1-4
# (jwt_x5c b64 payload binding).
# 2026-06-10: refreshed for v3.1 (spec/upgrade-plan-v3.1.md):
#   * Show-path unification: in-circuit challenge digest RETIRED; unified
#     credential_type-keyed link tag; epoch is a single Field.
#   * Passport commitment is arity-8 commit_passport_v3_1 (full-width
#     SOD/DG1 hashes; truncated 32-byte pack removed).
#   * v1/v2 circuits (passport_verifier, data_integrity, disclosure,
#     prepare_link, show_link, device_binding) retired to circuits-legacy/ --
#     dropped from this baseline; ~1.28 MB removed from the bundle.
#   Headline gate wins: openac_show 1802 -> 647 (-64%), x509_show 549 -> 488.
# 2026-06-10 sec.7.3 + Phase 6: passport prepare 28596 -> 19997 (DG count 4->2 +
#   DSC_TBS_LEN 1536->512) -> phone-side core 11000 after splitting the cacheable
#   CSCA trust chain into dsc_chain (10111). compute_dsc_id ties the two proofs.
# Update this after confirmed improvements via: make bench-update-baseline

[meta]
created = "2026-04-01"
updated = "2026-06-10"
nargo_version = "1.0.0-beta.19"

# Minimum test counts per circuit (fail if below)
[tests]
# openac_core v3.1: show module rewritten (digest tests removed, unified
# link-tag + cross-domain replay tests added); profile claims pack/unpack
# tests replace passport_profile roundtrip; commit gains arity-8 tests.
# openac_core: +4 Phase 6 compute_dsc_id tests + 4 sec.7.1 scoped link_rand -> 103.
openac_core = { positive = 61, negative = 3, total = 103 }
# openac_show v3.1: legacy digest tests removed; adds stale-nonce, forged
# claims, wrong-link-tag, CRITICAL-2 sentinel regressions.
openac_show = { positive = 8, negative = 8, total = 16 }
# Phase 6: dsc_chain is the cacheable CSCA trust-chain proof; passport_adapter
# slimmed to the per-holder core (DSC->SOD + DG + commitment + dsc_id pin).
dsc_chain = { positive = 5, negative = 7, total = 12 }
passport_adapter = { positive = 15, negative = 3, total = 18 }
sdjwt_adapter = { positive = 15, negative = 8, total = 23 }
jwt_x5c_adapter = { positive = 13, negative = 13, total = 26 }
# x509_show v3.1: digest negative test replaced by stale-nonce regression.
x509_show = { positive = 4, negative = 7, total = 11 }
# composite_show v3.1: +unlinkable positive; digest negative removed.
composite_show = { positive = 3, negative = 11, total = 14 }
mdoc_adapter = { positive = 13, negative = 15, total = 28 }

[tests.total]
minimum = 251

# Gate count baselines (ACIR opcodes, warn if > 10% regression)
# Measured: 2026-06-10, nargo 1.0.0-beta.19 (`nargo info --workspace`,
# main-function ACIR column).
[gates]
# Phase 6 split (sec.7.2): cacheable CSCA trust chain (CSCA->DSC RSA + Merkle +
# revocation SMT) -> cacheable dsc_id, precomputable once per DSC.
dsc_chain = 10111
# Phase 6 core (-61% vs 28596): only the per-holder DSC->SOD RSA + DG chain +
# commitment + dsc_id pin run on the phone; the CSCA RSA chain, Merkle, and SMT
# moved to dsc_chain. +63 sec.7.1 scoped link_rand, +104 sec.7.4 DG15 ECDSA-P256
# active authentication (ACIR blackbox; backend cost ~ one P256 verify).
passport_adapter = 11167
# v3.1 (-64% vs 1802): 2-block SHA256 challenge digest, epoch byte/field
# equality, and the packed-profile unpack/re-bind loops all removed.
openac_show = 647
sdjwt_adapter = 35255
# sec.7.3 windowing (-74% vs 136440): the P1-4 base64url decode + byte-equality
# now cover only the claim window [0,48) of the payload, not all 1024 bytes.
jwt_x5c_adapter = 35329
# v3.1 (-11% vs 549): digest computation removed; unified tag.
x509_show = 488
# v3.1 (+1.5% vs 815): arity-8 passport opening slightly outweighs the
# removed composite digest. Acceptable -- still tiny.
composite_show = 827
mdoc_adapter = 692381

# Artifact sizes in bytes (warn if > 20% regression)
# Measured: 2026-06-10, nargo 1.0.0-beta.19 (circuits/target/*.json).
[artifact_bytes]
dsc_chain = 664739
passport_adapter = 769483
openac_show = 146805
sdjwt_adapter = 792963
jwt_x5c_adapter = 1750450
x509_show = 125312
composite_show = 150762
mdoc_adapter = 10119930
# 2026-06-10 v3.1 + Phase 6: 16.0 MB across 8 bin circuits. The phone-side
# prepare footprint dropped (passport core 1.01 -> 0.75 MB); dsc_chain (0.66 MB)
# is a separate proof that is cacheable / server-distributed with the CSCA
# snapshot, so it does not have to ship in the phone bundle. sec.7.3 windowing
# then cut jwt_x5c_adapter 3.24 -> 1.75 MB.
total = 14520444

# Bytes per gate (lower = more compact artifact representation)
# Grade: A(≤10) B(≤30) C(≤60) D(≤100) F(>100)
# NOTE: tiny show circuits (x509_show / composite_show / openac_show) grade F
# on this ratio by construction -- low gate count inflates bytes/gate; their
# absolute artifact size (<150 KB each) is what matters for the bundle.
[bytes_per_gate]
dsc_chain = 66
passport_adapter = 69
openac_show = 227
sdjwt_adapter = 22
jwt_x5c_adapter = 50
x509_show = 257
composite_show = 182
mdoc_adapter = 15

# ============================================================
# Witness generation time (nargo execute) -- prove-time lower bound.
# 2026-06-10: the 2026-04-27 numbers covered only v1/v2 circuits, all of
# which are now retired to circuits-legacy/. v3.1 circuits need fresh
# Prover.toml fixtures before this section can be repopulated (Phase 0 of
# spec/upgrade-plan-v3.1.md).
# ============================================================
[witness_gen_ms]
status = "pending_v3_1_fixtures"

# ============================================================
# Real prove + verify timings.
#
# Toolchain: unblocked 2026-04-27 (noir_rs v1.0.0-beta.19; see git history
# for the migration notes). The only measured datapoint so far is the
# RETIRED v1 disclosure circuit -- kept as a reference magnitude because it
# demonstrates how heavily SHA256 blackboxes dominate mobile prove time
# (487 ACIR opcodes but 26.7 s prove / +309 MB RSS):
#   disclosure_prove_ms = 26690, disclosure_verify_ms = 83,
#   proof 18,724 B, vk 3,680 B, peak RSS +309 MB (darwin/arm64).
#
# v3.1 priority (upgrade-plan Phase 0): add bench wrappers in
# mopro-binding/src/noir.rs for openac_show / x509_show / composite_show /
# passport_adapter and record their prove/verify/RSS here. openac_show now
# carries ZERO SHA256 blocks; its remaining prove cost is dominated by the
# ECDSA-P256 device-binding blackbox.
# ============================================================
[performance]
status = "wrappers_pending_for_v3_1"
mopro_binding_noir_rs = "v1.0.0-beta.19"
host = "darwin/arm64"
