[package]
name = "mdoc_adapter"
type = "bin"
authors = ["kidneyweakx"]
description = "OpenAC mDoc/mDL adapter (v0): per-claim CBOR digest verification + Pedersen v3 commitment (Path A)"

[dependencies]
openac_core = { path = "../openac_core" }
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
