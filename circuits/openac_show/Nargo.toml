[package]
name = "openac_show"
type = "bin"
authors = ["kidneyweakx"]
description = "Generic OpenAC show circuit: re-verifies commitment + challenge binding + scoped linking"

[dependencies]
openac_core = { path = "../openac_core" }
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
