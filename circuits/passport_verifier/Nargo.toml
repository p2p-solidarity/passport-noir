[package]
name = "passport_verifier"
type = "bin"
authors = ["kidneyweakx"]
description = "ZK passport signature verification circuit using RSA-SHA256 (PKCS#1 v1.5)"

[dependencies]
noir_rsa = { tag = "v0.10.0", git = "https://github.com/zkpassport/noir_rsa" }
bignum = { tag = "v0.9.2", git = "https://github.com/noir-lang/noir-bignum" }
