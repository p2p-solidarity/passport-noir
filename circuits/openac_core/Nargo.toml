[package]
name = "openac_core"
type = "lib"
authors = ["kidneyweakx"]
description = "Generic OpenAC commitment, linking, and predicate library for multi-credential ZK proofs"

[dependencies]
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
