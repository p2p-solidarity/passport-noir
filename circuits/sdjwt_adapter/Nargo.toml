[package]
name = "sdjwt_adapter"
type = "bin"
authors = ["kidneyweakx"]
description = "OpenAC SD-JWT adapter: ES256 ECDSA verification + claim hash verification + Pedersen commitment"

[dependencies]
openac_core = { path = "../openac_core" }
sha256 = { tag = "v0.3.0", git = "https://github.com/noir-lang/sha256" }
